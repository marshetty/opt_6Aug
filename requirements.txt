# --- core maths / data ---
numpy>=2.3,<2.4
pandas>=2.3,<2.4          # built against NumPy 2.3+

# --- web app & charts ---
streamlit>=1.47,<2.0
streamlit-autorefresh>=1.0,<2.0
plotly==6.2.0             # includes plotly.express

# --- market data & scraping ---
tvDatafeed>=1.5,<1.6      # ← NEW: provides the TvDatafeed class
cloudscraper>=1.2.71      # optional but keeps NSE handshake robust
# yfinance, tradingview-datafeed no longer required

# --- networking / SSL ---
requests>=2.31
urllib3>=2.2
certifi>=2025.6
